# Backend server port
PORT=8080

# Option A: Single MySQL DSN (takes precedence if set)
# Format: user:pass@tcp(host:port)/dbname?parseTime=true&loc=Local
MYSQL_DSN=root:@tcp(127.0.0.1:3306)/fleetify_test?parseTime=true&loc=Local

# Option B: Split DB configuration (used when MYSQL_DSN is empty)
DB_USER=root
DB_PASS=
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=fleetify_test

# Notes:
# - The app will use MYSQL_DSN if provided, otherwise it builds DSN from DB_* values.
# - Ensure the database exists and the user has privileges.
# - Timezone-sensitive logic uses Asia/Jakarta for attendance timestamps.